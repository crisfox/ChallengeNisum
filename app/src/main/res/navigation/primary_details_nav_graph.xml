<?xml version="1.0" encoding="utf-8"?>
<navigation android:id="@+id/nav_graph"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/item_list_fragment">

    <fragment
        android:id="@+id/item_list_fragment"
        android:name="com.nisum.challenge.home.ui.view.ItemListFragment"
        android:label="Home Nisum">
        <action
            android:id="@+id/show_item_detail"
            app:destination="@id/item_detail_fragment"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@android:anim/fade_out"
            app:popEnterAnim="@android:anim/fade_in"
            app:popExitAnim="@android:anim/fade_out"
            app:popUpTo="@id/item_list_fragment"
            app:popUpToInclusive="false" />
    </fragment>
    <fragment
        android:id="@+id/item_detail_fragment"
        android:name="com.nisum.challenge.home.ui.view.ItemDetailFragment"
        android:label="Detail"
        tools:layout="@layout/fragment_item_detail">
        <argument
            android:name="item_id"
            app:argType="com.nisum.challenge.common.models.PokeModel[]" />
    </fragment>
</navigation>